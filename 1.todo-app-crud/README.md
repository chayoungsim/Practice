# React + TypeScript + Vite

```
npm create vite@latest todo-app-crud -- --template react-ts
cd todo-app-crud
npm install

// 이 패키지를 설치하면 Vite가 자동으로 .scss 파일을 인식하고 컴파일합니다.
npm install -D sass

```
    